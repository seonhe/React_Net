#include <stdio.h>

// filter (out_channels, in_channels, kernel_size, kernel_size)
// input (in_channels, in_size, in_size)
// output (out_channels, (in_size-kernel_size+padding*2)/stride +1, '')

void binary_Conv(int in_channels, int out_channels, int in_size, int kernel_size, int padding, int stride, float* parameter, float* input, float* output){
    
}

void real_Conv(int in_channels, int out_channels, int in_size, int kernel_size, int padding, int stride, float* parameter, float* input, float* output){

}